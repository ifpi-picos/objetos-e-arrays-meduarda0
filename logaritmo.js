function calcularLogaritmo() {
    const numero = Number(prompt('Digite o número que deseja calcular o logaritmo: '))
    const number = Math.log(numero)
    console.log(number)

};
calcularLogaritmo()